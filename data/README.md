# Data

| Path | Purpose |
|------|---------|
| `test_skus.json` | Small SKU set for development |
| `reference_skus.json` | Reference SKUs for benchmark comparison |
| `production_skus.json` | Production SKU metadata |
| `subset_*_skus.xlsx` | Excel subsets for incremental testing |
| `images/` | Runtime image cache (gitignored; populated by pipeline) |

Place client pricelist Excel files in the repo root locally (gitignored).
